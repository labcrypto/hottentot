#include "protocol_v1.h"


namespace naeem {
  namespace hottentot {
    namespace runtime {
      unsigned char* 
      ProtocolV1::SerializeRequest(Request *requet, 
                                   uint32_t *length) {
        // TODO(kamran)
      }
      Response* 
      ProtocolV1::DeserializeResponse(unsigned char *data, 
                                      uint32_t length) {
        // TODO(kamran)
      }
    }
  }
}