/******************************************************************
 * Generated by Hottentot JAVA Generator
 * Date: 
 * Name: 
 * Description:
 *   This file contains definition of an abstract service class.
 ******************************************************************/
package example.generated.hotgen;

import ir.ntnaeem.hottentot.serializerHelper.PDTSerializer;
import ir.ntnaeem.hottentot.serializerHelper.PDTDeserializer;
import ir.ntnaeem.hottentot.serializerHelper.ByteArrayToInteger;

public class Gender {
  private GenderType genders;
  private byte id;
  public void setGenders(GenderType genders) {
    this.genders = genders;
  }
  public GenderType getGenders() {
    return genders;
  }
  public void setId(byte id) {
    this.id = id;
  }
  public byte getId() {
    return id;
  }
  @Override 
  public String toString() { 
    return "Gender{" + 
      "genders = '" + genders + '\'' + 
      ",id = '" + id + '\'' + 
      "}"; 
  }
	
  public byte[] serialize() {
    byte[] serializedGenders = genders.serialize();
    byte[] serializedId = PDTSerializer.getInt8(id);
    byte[] output = new byte[serializedGenders.length + serializedId.length];
    int counter = 0;
    //use a loop for every property
    for (int i = 0; i < serializedGenders.length; i++) {
      output[counter++] = serializedGenders[i];
    }
    for (int i = 0; i < serializedId.length; i++) {
      output[counter++] = serializedId[i];
    }
    return output;
  }
    
  public void deserialize(byte[] serializedByteArray) {
    if(serializedByteArray.length != 0){
      int counter = 0;
      int dataLength = 0;
      int numbersOfBytesForDataLength;
      //do for every property
    //genders : GenderType
    byte[] gendersByteArray = new byte[2];
    for(int i = 0 ; i < 2 ; i++){
      gendersByteArray[i] = serializedByteArray[counter++];
    }
    setGenders(GenderType.deserialize(gendersByteArray));
    //id : byte
    byte[] idByteArray = new byte[1];
    for(int i = 0 ; i < 1 ; i++){
      idByteArray[i] = serializedByteArray[counter++];
    }
    setId(PDTDeserializer.getInt8(idByteArray));

    }
  }
}
