/******************************************************************
 * Generated by Hottentot JAVA Generator
 * Date: 
 * Name: 
 * Description:
 *   This file contains definition of an abstract service class.
 ******************************************************************/
package example.generated.hotgen;

import ir.ntnaeem.hottentot.serializerHelper.PDTSerializer;
import ir.ntnaeem.hottentot.serializerHelper.PDTDeserializer;

public class GenderWrapper {
  private GenderType gender;
  private byte id;
  public void setGender(GenderType gender) {
    this.gender = gender;
  }
  public GenderType getGender() {
    return gender;
  }
  public void setId(byte id) {
    this.id = id;
  }
  public byte getId() {
    return id;
  }
  @Override 
  public String toString() { 
    return "GenderWrapper{" + 
      "gender = '" + gender + '\'' + 
      ",id = '" + id + '\'' + 
      "}"; 
  }
	
  public byte[] serialize() {
    byte[] serializedGender = gender.serialize();
    byte[] serializedId = PDTSerializer.getInt8(id);
    byte[] output = new byte[serializedGender.length + serializedId.length];
    int counter = 0;
    //use a loop for every property
    for (int i = 0; i < serializedGender.length; i++) {
      output[counter++] = serializedGender[i];
    }
    for (int i = 0; i < serializedId.length; i++) {
      output[counter++] = serializedId[i];
    }
    return output;
  }
    
  public void deserialize(byte[] serializedByteArray) {
    if(serializedByteArray.length != 0){
      int counter = 0;
      int dataLength = 0;
      int numbersOfBytesForDataLength;
      //do for every property
    //gender : GenderType
    byte[] genderByteArray = new byte[2];
    for(int i = 0 ; i < 2 ; i++){
      genderByteArray[i] = serializedByteArray[counter++];
    }
    setGender(GenderType.deserialize(genderByteArray));
    //id : byte
    byte[] idByteArray = new byte[1];
    for(int i = 0 ; i < 1 ; i++){
      idByteArray[i] = serializedByteArray[counter++];
    }
    setId(PDTDeserializer.getInt8(idByteArray));

    }
  }
}
